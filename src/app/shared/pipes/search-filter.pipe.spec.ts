/* tslint:disable:no-unused-variable */

import {SearchFilterPipe} from './search-filter.pipe';

describe('Pipe: SearchFiltere', () => {
  it('create an instance', () => {
    let pipe = new SearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
